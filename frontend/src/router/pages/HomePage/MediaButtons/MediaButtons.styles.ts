import styled from 'styled-components'

export const MediaButtonsWrapper = styled.div`
  display: flex;
  gap: 10px;
  position: absolute;
  top: 1rem;
  right: 5rem;
  z-index: 10;
  display: flex;
  align-items: center;
  gap: 1.2rem;
`
